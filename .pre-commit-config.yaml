repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.8
    hooks:
      - id: ruff          # linter — auto-fixes what it can
        args: [--fix]
      - id: ruff-format   # formatter (replaces black)
